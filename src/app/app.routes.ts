import { Routes } from '@angular/router';
import { ButtonExamplesComponent } from './button-examples/button-examples';

export const routes: Routes = [
  { path: 'button-examples', component: ButtonExamplesComponent },
];
